# PetaLinux 2024.1 CONTAINER module image build — recover-or-rebuild.
#
# SOURCE OF TRUTH: this file lives in the infra repo
# (deployment/argocd-apps/eda/petalinux-2024-1/.gitlab-ci.yml). The build-trigger
# (postsync-build-image.yaml) MIRRORS it (+ Dockerfile + module.yaml) into the in-cluster
# GitLab project deployments/infrastructure/eda/modules/petalinux-2024-1; ArgoCD then
# TRIGGERS this pipeline. Do NOT hand-edit it in GitLab — overwritten on the next mirror.
#
# Its OWN GitLab project, like every other module: one project per app, so two triggers can
# never overwrite each other's mirror. module.yaml's `image:` key points the broker at this
# repo.
#
# Runs on unibi-hclab-pcie-tb-s via tags:[eda]. The PetaLinux media is a single self-extracting
# .run installer sitting in the SAME flat directory as the Vivado SDI tarballs on the TrueNAS
# NFS export, mounted READ-WRITE at /artifacts by the [eda] runner (static PV,
# gitlab-runner-eda/nfs-installers-pv.yaml) — see $MEDIA_DIR below. buildah bind-mounts the
# staged copy BUILD-TIME only (never committed to a layer).
stages: [build]

workflow:
  auto_cancel:
    on_new_commit: interruptible

build-petalinux-2024-1:
  stage: build
  tags: [eda]                     # -> pcie-tb-s [eda] runner (gitlab-runner-token-eda)
  # ⚠ THE SHARED CI BUILD IMAGE, not quay.io/buildah/stable: buildah to BUILD plus skopeo
  # for the registry gate below, which quay's image has never shipped. Built by
  # deployment/argocd-apps/ci-build-image — read its README before changing this line.
  # ⚠ The tag must equal that app's IMAGE_TAG; remote-desktop/check-image-invariants.sh
  # fails the commit if they drift. A consumer pinned to a tag that was never built simply
  # never starts its build pod.
  image: $CI_REGISTRY/deployments/infrastructure/ci/build-image:v1.43.2-skopeo-r1
  # Serialize rebuilds of THIS app. resource_group is PROJECT-scoped; the staged media on
  # /build-scratch is additionally guarded by an flock (see the stage step below).
  resource_group: petalinux-2024-1-build
  interruptible: true
  # ── MEASURED COSTS (first green pipeline, 2026-09-04, ecc197) ────────────────────────
  #     stage the .run from NFS   <1 min    (3.1 GB off the artifacts export; skipped
  #                                          entirely on a re-run, the marker keeps it)
  #     apt + dash + useradd      ~1 min
  #     the .run itself           2m00s     (19:21:43 -> 19:23:43, all three platforms)
  #     asserts + shims           <1 min
  #     push to registry          1m58s     (19:26:58 -> 19:28:56, one attempt)
  #     TOTAL                     ~8 min    pod start to pushed
  # ⚠ THE INSTALL IS MINUTES, NOT HOURS. This app is nothing like the Vivado ones: no
  # Xvfb, no xsetup, no 115 GB of media. Do not copy their 3-6 h budgets here.
  # The [eda] runner is shared with those builds though, so a queued xilinx build is what
  # actually delays this one.
  #
  # DISK on /build-scratch: 3.1 GB staged media plus buildah storage for an 11 GB installed
  # tree. The `--platform` choice barely moves that number — see the size breakdown in the
  # README: 9.8 GB of the 11 GB is `components/xsct`, which every platform shares, and all
  # THREE platform source sets together are 50 MB. Far below the Vivado peak (~450 GB), which is why the guard below warns
  # at a much lower number.
  timeout: 4h
  variables:
    FORCE_REBUILD: "0"                 # ONE-SHOT: bitbake now inherits a PATH with the SDK
                                       # sysroot stripped, so Yocto stops capturing the SDK's
                                       # GLIBC_2.36 binaries as HOSTTOOLS (2026-09-20).
                                       # ⚠ RESET TO "0" once published and registered.
                                       # set to "1" for a ONE-SHOT rebuild of the same tag after a
                                       # Dockerfile fix (IMAGE_TAG is the user-facing module version
                                       # and must not be bumped); reset to 0 once published.
                                       # ⚠ checkForceRebuildReset.py FAILS precommit while it is
                                       # left at "1" — deliberate; the reset gets forgotten.
                                       # set to "1" for a ONE-SHOT rebuild of the same tag after a
                                       # Dockerfile fix (IMAGE_TAG is the user-facing module version
                                       # and must not be bumped); reset to 0 once published.
                                       # ⚠ checkForceRebuildReset.py FAILS precommit while it is
                                       # left at "1" — that is deliberate, the reset is the half
                                       # that gets forgotten.
                                       # set to "1" for a ONE-SHOT rebuild of the same tag after a
                                       # Dockerfile fix (IMAGE_TAG is the user-facing module version
                                       # and must not be bumped); reset to 0 once published
    IMAGE_TAG: "2024.1"                # the MODULE version (module load petalinux/2024.1)
    IMAGE: $CI_REGISTRY_IMAGE:$IMAGE_TAG
    # ⚠ REPO PATH ONLY — no host, and NO :TAG. `${IMAGE#*/}` strips the host but LEAVES
    # the tag, so a /v2/<repo>/manifests/<tag> URL built from it asks for a repository
    # literally named "…/<name>:<tag>", which cannot exist -> 404 forever. The gate is
    # fail-open, so that 404 reads as "not in the registry" and rebuilds from installer
    # media every single time, silently.
    # A `docker://` push destination is the opposite case — it MUST keep the tag, so those
    # still use ${IMAGE#*/}.
    IMAGE_REPO: $CI_PROJECT_PATH
    # ── installer media on the NFS export ────────────────────────────────────────────
    # ONE FLAT DIRECTORY per vendor on the fast export, mounted rw at /artifacts by the
    # [eda] runner (static PV, gitlab-runner-eda/nfs-installers-pv.yaml). This is the SAME
    # directory the two Vivado apps read their SDI tarballs from — PetaLinux is an AMD
    # product and its media is staged there beside them.
    MEDIA_DIR: /artifacts/xilinx
    #
    # ⚠ NAMED IN FULL, not globbed, for the same reason the Vivado apps name theirs: the
    # directory is shared, so `ls $MEDIA_DIR/petalinux*.run | head -1` becomes a lottery the
    # day a second PetaLinux version is staged — and the loser builds silently against the
    # wrong media, publishing it under this app's user-facing module tag. Update this when
    # the media file is replaced.
    MEDIA_RUN: petalinux-v2024.1-05202009-installer.run
    #
    # Module identity, for the gates in the build trigger. Declared separately from
    # IMAGE_TAG on purpose: the two coincide today, but a tag that ever diverged from the
    # module version would silently check the wrong module.
    MODULE_NAME: "petalinux"
    MODULE_VERSION: "2024.1"
    REGISTRY_TLS_VERIFY: "true" # automatically updated from project-settings:{tls.certIssuerType,tls.registryTlsVerify}
    STORAGE_DRIVER: overlay
    BUILDAH_ROOT: /build-scratch/buildah-storage
    BUILDAH_RUNROOT: /build-scratch/buildah-run
    # ⚠ THE LAB-LOCAL REGISTRY, not GitLab's. EDA module images are published here and
    # ONLY here; GitLab's registry stays the home for everything else (remote-desktop
    # base, vLLM, …). See deployment/argocd-apps/image-registry/README.md.
    #
    # AUTHENTICATED (htpasswd) and TLS from a PRIVATE in-cluster CA. Both are required,
    # not hardening: what this registry serves is executed AS ROOT by the remote-desktop
    # broker, whose allowed() gate authorises by <repo>:<tag> and never checks the CONTENT
    # at that tag. The credential arrives as masked CI variables and the CA as
    # IMAGE_REGISTRY_CA, both published by this app's build-trigger.
    INTERNAL_REGISTRY: image-registry.image-registry.svc.cluster.local:5000
  script:
    # Trust the image-registry CA. containers/image reads
    # /etc/containers/certs.d/<host>:<port>/*.crt automatically, so writing it here
    # means every later buildah call verifies TLS with NO per-call flag — and a missing
    # CA fails loudly at handshake instead of silently downgrading.
    # ⚠ MUST STAY THE FIRST STEP: the registry gate below reads this file implicitly
    # (skopeo has no --cacert flag), so a later write means the gate fails open and every
    # run rebuilds from installer media. Enforced by
    # remote-desktop/check-image-invariants.sh.
    - |
      mkdir -p "/etc/containers/certs.d/${INTERNAL_REGISTRY}"
      printf '%s\n' "$IMAGE_REGISTRY_CA" > "/etc/containers/certs.d/${INTERNAL_REGISTRY}/ca.crt"
      # ⚠ NOT openssl — the CI build image is Fedora-based and does NOT ship it. python3 IS
      # present, and a stdlib-only parse keeps the fail-loud intent with no package install.
      python3 - "/etc/containers/certs.d/${INTERNAL_REGISTRY}/ca.crt" <<'CACHECK' \
        || { echo "ERROR: IMAGE_REGISTRY_CA is not a valid certificate"; exit 1; }
      import base64, re, sys
      raw = open(sys.argv[1], "rb").read().decode("utf-8", "replace")
      m = re.search(r"-----BEGIN CERTIFICATE-----(.+?)-----END CERTIFICATE-----", raw, re.S)
      if not m:
          sys.exit("no PEM CERTIFICATE block")
      try:
          der = base64.b64decode("".join(m.group(1).split()), validate=True)
      except Exception as exc:
          sys.exit(f"base64 decode failed: {exc}")
      # 0x30 = ASN.1 SEQUENCE, the first byte of every X.509 certificate.
      if len(der) < 64 or der[0] != 0x30:
          sys.exit("does not parse as a DER certificate")
      print(f"image-registry CA OK ({len(der)} bytes DER)")
      CACHECK
    - |
      # Recover-or-rebuild: skip if the tag already exists in the registry.
      #
      # IMAGE_TAG is the USER-FACING module version (`module load petalinux/2024.1`), so it
      # cannot be bumped just to force a rebuild after a Dockerfile fix — that would rename
      # the module. Set FORCE_REBUILD=1 (pipeline variable, or here) to rebuild the same tag
      # in place; the push overwrites it.
      if [ "${FORCE_REBUILD:-0}" = "1" ]; then
        echo "FORCE_REBUILD=1 — rebuilding $IMAGE even though it may already exist."
      # ⚠ ASK THE REGISTRY WE ACTUALLY PUSH TO — not GitLab's API. EDA module images are
      # published to the lab-local $INTERNAL_REGISTRY, so GitLab's API reports the tag
      # ABSENT forever and every pipeline would rebuild from installer media.
      #
      # `skopeo inspect --raw` performs the registry's auth dance, sends the Accept header
      # for every manifest media type, and verifies TLS against the certs.d file the step
      # above writes — which is why THAT STEP MUST STAY FIRST.
      #
      # Still FAIL-OPEN: anything but a clean success means "treat as absent and build",
      # which costs a rebuild, never a wrong image.
      else
        # ⚠ PRINT WHAT WENT WRONG. This gate is fail-open by design, so every failure mode
        # otherwise looks identical to "not in the registry" and costs a multi-hour rebuild
        # with no error anywhere. skopeo's own message distinguishes them (authentication
        # required = credential, manifest unknown = genuinely absent, x509 = CA, dial tcp =
        # DNS/connect).
        #
        # Credentials empty is NOT a soft failure — it means the CI variables did not reach
        # this pipeline (see the protected=true note in this app's build trigger), so every
        # run would rebuild forever. Fail loudly instead of silently burning hours.
        if [ -z "${IMAGE_REGISTRY_USER:-}" ] || [ -z "${IMAGE_REGISTRY_PASSWORD:-}" ]; then
          echo "ERROR: IMAGE_REGISTRY_USER/PASSWORD are empty in this pipeline."
          echo "       The registry gate cannot run, so this build would restore nothing and"
          echo "       rebuild from installer media on EVERY run."
          echo "       Cause: the CI variables are published unprotected while this ref is"
          echo "       protected (CI_COMMIT_REF_PROTECTED=${CI_COMMIT_REF_PROTECTED:-unset})."
          echo "       Fix: publish them with protected=true in this app's build trigger."
          exit 1
        fi
        # ⚠ REPORT THE REF THAT WAS ACTUALLY CHECKED. $IMAGE names the GitLab registry, but
        # module images live ONLY in $INTERNAL_REGISTRY.
        _gate_ref="${INTERNAL_REGISTRY}/${IMAGE#*/}"
        set +e
        _gate_out=$(skopeo inspect --raw \
                 --creds "$IMAGE_REGISTRY_USER:$IMAGE_REGISTRY_PASSWORD" \
                 "docker://${_gate_ref}" 2>&1)
        _gate_rc=$?
        set -e
        if [ "$_gate_rc" -eq 0 ]; then
          echo "$_gate_ref already exists — skipping build."
          exit 0
        fi
        case "$_gate_out" in
          *"manifest unknown"*|*"name unknown"*|*"was not found"*)
            echo "registry gate: tag not present in $INTERNAL_REGISTRY — building." ;;
          *"authentication required"*|*"unauthorized"*)
            echo "registry gate: AUTH REJECTED by $INTERNAL_REGISTRY. The credential is wrong,"
            echo "  not the tag. Building anyway (fail-open), but the recreate-survival path is"
            echo "  NOT working — fix the credential."
            echo "  skopeo: $_gate_out" ;;
          *x509*|*certificate*)
            echo "registry gate: TLS verification failed against $INTERNAL_REGISTRY. Is"
            echo "  IMAGE_REGISTRY_CA the CA of the CURRENT cluster? A recreate mints a new one."
            echo "  skopeo: $_gate_out" ;;
          *)
            echo "registry gate: unexpected failure — treating as absent, building."
            echo "  skopeo: $_gate_out" ;;
        esac
      fi
      echo "Building $IMAGE on $(uname -m) at $(date -u +%F' '%T)..."
    - |
      # Stage the .run onto node-local /build-scratch.
      #
      # ⚠ A COPY, NOT A BIND MOUNT OF $MEDIA_DIR. Two reasons, both hard:
      #   - the installer must be EXECUTABLE, and the export is not ours to chmod (the same
      #     directory holds the two Vivado SDI tarballs);
      #   - bind-mounting $MEDIA_DIR would put 115 GB of unrelated Vivado media inside the
      #     build container, where a glob could pick the wrong file.
      # Only the ONE named file is copied, into a directory of this app's own.
      #
      # Idempotent: marker written LAST, so a partial copy is redone. The flock guards
      # against a second consumer of the same scratch path; resource_group only serialises
      # THIS project.
      set -e
      SRC=$MEDIA_DIR/$MEDIA_RUN
      STAGE=/build-scratch/petalinux-2024-1-media
      LOCK=/build-scratch/.petalinux-2024-1-stage.lock
      touch "$LOCK"
      flock -w 3600 "$LOCK" sh -euc '
        STAGE="$1"; SRC="$2"; RUNF="$3"; MEDIA_DIR="$4"
        if [ -f "$STAGE/.stage-done" ] && [ -x "$STAGE/$RUNF" ]; then
          echo "Installer already staged at $STAGE/$RUNF."
          exit 0
        fi
        [ -f "$SRC" ] || {
          echo "ERROR: installer media not found at $SRC — has it been staged onto the NFS"
          echo "       export, and does \$MEDIA_RUN still name the file that is there?"
          ls -la "$MEDIA_DIR" 2>&1 | head -20; exit 1; }
        avail_gb=$(df -BG --output=avail /build-scratch | tail -1 | tr -dc "0-9")
        # ⚠ A WARN, not a gate: a tight-but-sufficient disk should still build, so the
        # message is the only signal. 3.1 GB media + buildah storage for an 11 GB tree.
        [ "$avail_gb" -ge 80 ] || echo "WARN: only ${avail_gb}G free on /build-scratch — this build needs roughly 40G (3G media + buildah storage for an 11G tree). It may fail on ENOSPC."
        echo "[$(date -u +%F" "%T)] staging $SRC (3.1 GB at the measured ~126 MB/s NFS read)..."
        rm -rf "$STAGE"; mkdir -p "$STAGE"
        cp "$SRC" "$STAGE/$RUNF"
        chmod 0755 "$STAGE/$RUNF"
        touch "$STAGE/.stage-done"
        echo "[$(date -u +%F" "%T)] staging done."
      ' sh "$STAGE" "$SRC" "$MEDIA_RUN" "$MEDIA_DIR"
      # Re-assert outside the lock: if another job staged it, this is what we build against.
      [ -x "$STAGE/$MEDIA_RUN" ] || { echo "ERROR: $STAGE/$MEDIA_RUN missing or not executable after the stage step"; exit 1; }
    # BOTH registries: $CI_REGISTRY for the Dockerfile's base-image pull, and
    # $INTERNAL_REGISTRY (image-registry) for the push — separate hosts, separate creds.
    - buildah login --tls-verify=$REGISTRY_TLS_VERIFY -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    - buildah login -u "$IMAGE_REGISTRY_USER" -p "$IMAGE_REGISTRY_PASSWORD" "$INTERNAL_REGISTRY"
    - mkdir -p "$BUILDAH_ROOT" "$BUILDAH_RUNROOT"
    - echo "[$(date -u +%F' '%T)] buildah bud starting (the .run installer runs inside, expect ~30 min)..."
    # -v bind-mounts the staged installer BUILD-TIME only (never committed to a layer).
    # --root/--runroot put layer storage on the node-local scratch hostPath.
    - buildah --root "$BUILDAH_ROOT" --runroot "$BUILDAH_RUNROOT" --storage-driver "$STORAGE_DRIVER"
      bud --isolation chroot --format docker
      -v /build-scratch/petalinux-2024-1-media:/installer:ro
      -t "$IMAGE" .
    - |
      # Push under the SAME repository path as $IMAGE (pods/broker pull via the
      # ingress name). PreferClose keeps bytes on the lab LAN; retries resume per-blob.
      DEST="docker://${INTERNAL_REGISTRY}/${IMAGE#*/}"
      set +e
      n=0
      until [ "$n" -ge 6 ]; do
        echo "=== push attempt $((n+1)) START $(date -u +%H:%M:%S) ==="
        timeout 10800 buildah --root "$BUILDAH_ROOT" --runroot "$BUILDAH_RUNROOT" --storage-driver "$STORAGE_DRIVER" \
          push --retry 20 --retry-delay 10s \
          "$IMAGE" "$DEST"
        rc=$?
        echo "=== push attempt $((n+1)) END $(date -u +%H:%M:%S) rc=$rc ==="
        [ "$rc" -eq 0 ] && break
        n=$((n+1)); echo "push attempt $n failed (rc=$rc) — retrying in 30s (buildah resumes from uploaded blobs)..."; sleep 30
      done
      set -e
      [ "$n" -lt 6 ] || { echo "ERROR: push failed after 6 attempts"; exit 1; }
    - buildah --root "$BUILDAH_ROOT" --runroot "$BUILDAH_RUNROOT" --storage-driver "$STORAGE_DRIVER" rmi --prune || true
    - echo "Pushed $IMAGE (via $INTERNAL_REGISTRY) at $(date -u +%F' '%T)"
  # ── ALWAYS-RUN SCRATCH RECLAIM ───────────────────────────────────────────────────────
  # Runs on SUCCESS AND FAILURE, which is the whole point: the `rmi --prune` at the end of
  # `script:` is unreachable when the build dies, and a failed `buildah bud` is exactly the
  # case that leaves the most behind. /build-scratch is the node-local hostPath
  # /var/lib/xilinx-build, shared with the Vivado builds; nothing else ever wipes it.
  #
  # WHAT IS DELETED: buildah's containers, images and layer cache, and only via buildah's
  # own subcommands — never `rm -rf $BUILDAH_ROOT`, which can leave overlay mounts behind
  # and break the next run. Order matters: `rm -a` first (a working container holds a
  # reference that makes `rmi` fail), then `rmi -a -f`, then `prune -a -f`.
  #
  # WHAT IS KEPT: $STAGE, the staged installer, guarded by .stage-done. Reclaim it by hand
  # when the disk is genuinely tight:
  #   rm -rf /var/lib/xilinx-build/petalinux-2024-1-media
  #
  # ⚠ NO `set -e` and every command `|| true`: after_script must not turn a green build red,
  # and on the failure path it must not mask the real error with a cleanup error. GitLab
  # gives after_script its own 5-min default timeout, so keep it cheap.
  after_script:
    - |
      echo "[$(date -u +%F' '%T)] scratch reclaim (job status: ${CI_JOB_STATUS:-unknown})"
      df -h /build-scratch || true
      BA="buildah --root $BUILDAH_ROOT --runroot $BUILDAH_RUNROOT --storage-driver $STORAGE_DRIVER"
      $BA rm -a || true
      $BA rmi -a -f || true
      $BA prune -a -f || true
      echo "[$(date -u +%F' '%T)] after reclaim:"
      df -h /build-scratch || true
      du -sh /build-scratch/* 2>/dev/null | sort -h || true
