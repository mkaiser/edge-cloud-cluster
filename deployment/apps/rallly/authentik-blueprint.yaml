# Authentik blueprint: Rallly OIDC provider + dashboard tile.
#
# Per-app blueprint — shipped by THIS app's ArgoCD Application into the authentik
# namespace and mounted optionally into the Authentik worker
# (deployment/infrastructure/authentik/values.yaml → worker.volumes: bp-rallly).
# If rallly is removed from the sync waves, this ConfigMap is never created and no
# Rallly tile appears.
#
# client_secret comes from the authentik-secrets sealed bundle (!Env). Redirect
# URI uses the cluster subdomain — rewritten by updateConfigFromProjectSettings.sh.
apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprint-rallly
  namespace: authentik
  labels:
    goauthentik.io/blueprint: "true"
data:
  rallly.yaml: |
    version: 1
    metadata:
      name: app-rallly
    entries:
      - id: provider-rallly
        model: authentik_providers_oauth2.oauth2provider
        identifiers:
          name: rallly
        attrs:
          client_type: confidential
          client_id: rallly
          client_secret: !Env RALLLY_OIDC_CLIENT_SECRET
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
          grant_types:
            - authorization_code
            - refresh_token
          authorization_flow:
            !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow:
            !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          redirect_uris:
            - matching_mode: strict
              url: https://rallly.ecc135.cape-project.eu/api/auth/callback/oidc
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
      - model: authentik_core.application
        identifiers:
          slug: rallly
        attrs:
          name: Rallly
          group: Office
          provider: !KeyOf provider-rallly
          meta_launch_url: https://rallly.ecc135.cape-project.eu
          meta_icon: https://cdn.jsdelivr.net/gh/selfhst/icons/svg/rallly.svg
      # Access control: employees / employees-extended / admins only.
      - model: authentik_policies.policybinding
        identifiers:
          target: !Find [authentik_core.application, [slug, rallly]]
          policy: !Find [authentik_policies_expression.expressionpolicy, [name, access-employee-apps]]
        attrs:
          order: 0
          enabled: true
