---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: headscale-pg-user
  namespace: headscale
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  encryptedData:
    password: ""
    username: ""
  template:
    metadata:
      name: headscale-pg-user
      namespace: headscale
